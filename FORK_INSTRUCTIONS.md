# 🍴 Инструкция по форку репозитория

## 📍 Исходный репозиторий

```
https://github.com/Vlad9572324/hh.ru-clicker.git
```

## 🔄 Процесс форка

### Шаг 1: Форк через веб-интерфейс GitHub

1. Откройте репозиторий в браузере:
   ```
   https://github.com/Vlad9572324/hh.ru-clicker
   ```

2. Нажмите кнопку **"Fork"** (справа вверху)

3. Выберите свой аккаунт GitHub для форка

4. Дождитесь завершения форка

### Шаг 2: Клонировать свой форк

После форка у вас будет свой репозиторий по адресу:
```
https://github.com/ВАШ_USERNAME/hh.ru-clicker.git
```

Клонируйте его:

```bash
# Замените ВАШ_USERNAME на ваш GitHub username
git clone https://github.com/ВАШ_USERNAME/hh.ru-clicker.git

# Или через SSH (если настроен)
git clone git@github.com:ВАШ_USERNAME/hh.ru-clicker.git
```

### Шаг 3: Настроить remote для работы с оригиналом

```bash
cd hh.ru-clicker

# Посмотреть текущие remote
git remote -v

# Добавить оригинальный репозиторий как upstream
git remote add upstream https://github.com/Vlad9572324/hh.ru-clicker.git

# Проверить
git remote -v
```

Теперь у вас будет:
- `origin` - ваш форк
- `upstream` - оригинальный репозиторий

## 🔄 Рабочий процесс с форком

### Получить изменения из оригинального репозитория

```bash
# Получить изменения из upstream
git fetch upstream

# Переключиться на main ветку
git checkout main

# Смержить изменения из upstream
git merge upstream/main

# Или использовать rebase
git rebase upstream/main

# Отправить в свой форк
git push origin main
```

### Внести свои изменения

```bash
# Создать новую ветку для изменений
git checkout -b feature/telegram-bot

# Внести изменения
# ...

# Добавить изменения
git add .

# Закоммитить
git commit -m "Add Telegram bot"

# Отправить в свой форк
git push origin feature/telegram-bot
```

### Создать Pull Request

1. Откройте свой форк на GitHub
2. Нажмите **"Compare & pull request"**
3. Выберите базовую ветку (upstream/main) и вашу ветку
4. Опишите изменения
5. Создайте Pull Request

## 📋 Быстрая настройка после форка

```bash
# Если у вас уже есть локальная копия с другим origin
cd hh.ru-clicker

# Изменить origin на ваш форк
git remote set-url origin https://github.com/ВАШ_USERNAME/hh.ru-clicker.git

# Добавить upstream
git remote add upstream https://github.com/Vlad9572324/hh.ru-clicker.git

# Проверить
git remote -v
```

## 🔍 Полезные команды

```bash
# Посмотреть все remote
git remote -v

# Обновить информацию о ветках
git fetch upstream
git fetch origin

# Посмотреть различия с upstream
git diff main upstream/main

# Синхронизировать свой форк с оригиналом
git checkout main
git fetch upstream
git merge upstream/main
git push origin main
```
