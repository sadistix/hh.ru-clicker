# ✅ Чеклист перед коммитом

## 🔒 Проверка безопасности

Перед каждым коммитом убедитесь:

- [ ] **Нет реальных токенов в коде**
  ```bash
  grep -r "TELEGRAM_BOT_TOKEN.*=" --include="*.py" --include="*.sh" --include="*.bat" . | grep -v "your_telegram_bot_token\|ваш_токен"
  ```

- [ ] **Нет токенов HH в коде**
  ```bash
  grep -r "hhtoken.*=" --include="*.py" . | grep -v "<token\|ваш_токен\|example"
  ```

- [ ] **Файлы с данными игнорируются**
  ```bash
  git check-ignore data/ bot.log .env
  ```

- [ ] **`.env.example` содержит только плейсхолдеры**
  ```bash
  cat .env.example | grep -v "your_telegram_bot_token\|your_token_here"
  ```

## 📝 Что можно коммитить

✅ **Безопасно:**
- Код приложения (`.py` файлы)
- Документация (`.md` файлы)
- Скрипты установки (`setup.sh`, `setup.bat`)
- Конфигурационные примеры (`.env.example`)
- `requirements.txt`
- `.gitignore`, `.gitattributes`

❌ **НЕ коммитить:**
- `data/` - все файлы в этой папке
- `bot.log` и другие логи
- `.env` - реальные переменные окружения
- `venv/` - виртуальное окружение
- Любые файлы с реальными токенами

## 🚀 Быстрая проверка

```bash
# Проверка перед коммитом
git status
git diff

# Убедитесь что нет чувствительных данных
git diff | grep -i "token\|password\|secret\|key" | grep -v "your_token\|example\|placeholder"
```

## 📋 Рекомендуемый порядок коммита

```bash
# 1. Проверьте изменения
git status

# 2. Добавьте файлы (исключая чувствительные)
git add .gitignore .gitattributes
git add *.py *.md *.sh *.bat *.txt
git add .env.example

# 3. Проверьте что будет закоммичено
git status

# 4. Создайте коммит
git commit -m "Описание изменений"

# 5. Отправьте в репозиторий
git push
```

## ⚠️ Если случайно добавили чувствительные данные

```bash
# Удалите из индекса
git rm --cached data/bot_config.json

# Проверьте .gitignore
cat .gitignore | grep data

# Исправьте и закоммитьте
git add .gitignore
git commit -m "Fix: add data/ to gitignore"
```
