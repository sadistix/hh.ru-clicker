# 🔄 Git Workflow

## 📍 URL репозитория

```
https://github.com/Vlad9572324/hh.ru-clicker.git
```

## 🚀 Рабочий процесс

### 1. Стянуть последние изменения

```bash
# Получить последние изменения из удалённого репозитория
git fetch origin

# Или сразу стянуть и смержить
git pull origin main
```

### 2. Проверить текущее состояние

```bash
# Посмотреть какие файлы изменены
git status

# Посмотреть изменения
git diff
```

### 3. Добавить изменения

```bash
# Добавить все безопасные файлы
git add .gitignore .gitattributes .env.example
git add *.py *.md *.sh *.bat *.txt
git add data/.gitkeep

# Или добавить все изменения (кроме игнорируемых)
git add -A
```

### 4. Проверить что будет закоммичено

```bash
# Убедитесь что нет чувствительных данных
git status
git diff --cached

# Проверка на токены
git diff --cached | grep -i "token\|password\|secret" | grep -v "your_token\|example"
```

### 5. Создать коммит

```bash
git commit -m "Add Telegram bot with browser automation

- Add telegram_bot.py with Playwright browser automation
- Add setup scripts (setup.sh, setup.bat)
- Add run scripts (run.sh, run.bat)
- Add comprehensive documentation
- Update .gitignore for security
- Add .gitattributes for file handling"
```

### 6. Отправить изменения

```bash
# Отправить в удалённый репозиторий
git push origin main

# Или просто
git push
```

## 🔄 Полный цикл работы

```bash
# 1. Стянуть изменения
git pull origin main

# 2. Внести изменения в код

# 3. Проверить изменения
git status
git diff

# 4. Добавить файлы
git add .

# 5. Закоммитить
git commit -m "Описание изменений"

# 6. Отправить
git push origin main
```

## ⚠️ Если есть конфликты

```bash
# Если при pull возникли конфликты
git pull origin main

# Разрешить конфликты в файлах
# Затем добавить разрешённые файлы
git add .

# Завершить merge
git commit

# Отправить
git push origin main
```

## 🔍 Полезные команды

```bash
# Посмотреть историю коммитов
git log --oneline

# Посмотреть изменения в последнем коммите
git show HEAD

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Посмотреть удалённые ветки
git branch -r

# Посмотреть все ветки
git branch -a
```
