# Быстрый старт Telegram бота

## 1. Установка (автоматическая)

**Linux/Mac:**
```bash
chmod +x setup.sh
./setup.sh
```

**Windows:**
```bash
setup.bat
```

Или вручную:

```bash
# Создать виртуальное окружение
python3 -m venv venv  # Linux/Mac
# или
python -m venv venv   # Windows

# Активировать
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate    # Windows

# Установить зависимости
pip install -r requirements.txt
playwright install chromium
```

## 2. Получение токена Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

## 3. Активация виртуального окружения

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

## 4. Запуск бота

```bash
export TELEGRAM_BOT_TOKEN='ваш_токен_от_BotFather'  # Linux/Mac
# или
set TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather        # Windows

python telegram_bot.py
```

## 4. Настройка через бота

1. Откройте бота в Telegram и отправьте `/start`
2. Нажмите "⚙️ Настройки"
3. Настройте:
   - **Токены HH** - введите в формате:
     ```
     hhtoken=ваш_токен
     hhul=ваш_токен
     crypted_id=ваш_токен
     xsrf=ваш_токен
     ```
   - **Resume Hash** - hash из URL резюме
   - **Сопроводительное письмо** - текст письма
   - **URL поиска** - ссылки на поиск вакансий

## 5. Использование

- **🚀 Запустить отклики** - автоматическая обработка вакансий
- **📤 Поднять резюме** - поднять резюме в поиске
- **📊 Статистика** - просмотр статистики

## Получение токенов HH

1. Зайдите на hh.ru
2. Нажмите F12 → Application → Cookies → https://hh.ru
3. Скопируйте значения: `hhtoken`, `hhul`, `crypted_id`, `_xsrf`
