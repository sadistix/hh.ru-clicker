# 🔒 Безопасность

## ⚠️ Важно: Защита личных данных

Этот репозиторий содержит код для автоматизации работы с hh.ru. **Никогда не коммитьте личные данные в git!**

## 🚫 Что НЕ должно попадать в git:

### ❌ Токены и пароли
- Токены Telegram бота (`TELEGRAM_BOT_TOKEN`)
- Токены HH.ru (`hhtoken`, `hhul`, `crypted_id`, `_xsrf`)
- Resume hash
- Любые другие токены и пароли

### ❌ Конфигурационные файлы с данными
- `data/bot_config.json` - содержит токены
- `data/applied_vacancies.json` - история откликов
- `data/stats.json` - статистика
- `.env` - переменные окружения с токенами
- `bot.log` - может содержать токены в логах

### ❌ Личная информация
- Сопроводительные письма с контактами
- URL поиска с личными фильтрами
- Любые другие персональные данные

## ✅ Что безопасно коммитить:

- Код приложения (`.py` файлы)
- Документация (`.md` файлы)
- Примеры конфигурации (`.env.example` с плейсхолдерами)
- Скрипты установки (`setup.sh`, `setup.bat`)
- `requirements.txt`

## 🛡️ Защита через .gitignore

Все чувствительные файлы автоматически игнорируются через `.gitignore`:
- `data/` - все данные пользователя
- `*.log` - логи
- `.env` - переменные окружения
- `venv/` - виртуальное окружение

## 📝 Перед коммитом проверьте:

```bash
# Проверьте, что нет чувствительных файлов
git status

# Проверьте содержимое файлов перед коммитом
git diff

# Убедитесь, что .env.example не содержит реальных токенов
cat .env.example
```

## 🔄 Если случайно закоммитили токены:

1. **Немедленно** отзовите токены:
   - Telegram: создайте нового бота у @BotFather
   - HH.ru: смените пароль и получите новые токены

2. Удалите токены из истории git:
   ```bash
   git filter-branch --force --index-filter \
     "git rm --cached --ignore-unmatch data/bot_config.json" \
     --prune-empty --tag-name-filter cat -- --all
   ```

3. Принудительно обновите удалённый репозиторий:
   ```bash
   git push origin --force --all
   ```

## 📚 Дополнительные ресурсы

- [GitHub: Удаление чувствительных данных](https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/removing-sensitive-data-from-a-repository)
- [Git: filter-branch](https://git-scm.com/docs/git-filter-branch)
