# Telegram бот для автоматизации откликов на hh.ru

Бот использует браузерную автоматизацию (Playwright) для реального взаимодействия с сайтом hh.ru через виртуализированный браузер.

## 🚀 Установка

### Вариант 1: Автоматическая установка (рекомендуется)

**Linux/Mac:**
```bash
chmod +x setup.sh
./setup.sh
```

**Windows:**
```bash
setup.bat
```

Скрипт автоматически:
- Создаст виртуальное окружение
- Установит все зависимости
- Установит браузеры для Playwright

### Вариант 2: Ручная установка

#### 1. Создайте виртуальное окружение

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

#### 2. Установите зависимости

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

#### 3. Установите браузеры для Playwright

```bash
playwright install chromium
```

### 3. Получите токен Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### 4. Настройте переменные окружения

Создайте файл `.env` или установите переменную окружения:

```bash
export TELEGRAM_BOT_TOKEN='ваш_токен_от_BotFather'
```

Или создайте файл `.env`:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

## 📋 Получение токенов HH

1. Зайдите на [hh.ru](https://hh.ru) под своим аккаунтом
2. Нажмите `F12`, откройте вкладку **Application**
3. В левой панели выберите `Cookies → https://hh.ru`
4. Найдите и скопируйте значения:
   - `hhtoken`
   - `hhul`
   - `crypted_id`
   - `_xsrf`

## 🎯 Использование

### Активация виртуального окружения

**Linux/Mac:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### Запуск бота

```bash
export TELEGRAM_BOT_TOKEN='ваш_токен'  # Linux/Mac
# или
set TELEGRAM_BOT_TOKEN=ваш_токен        # Windows

python telegram_bot.py
```

### Деактивация виртуального окружения

```bash
deactivate
```

### Настройка через бота

1. Запустите бота и отправьте команду `/start`
2. Нажмите "⚙️ Настройки"
3. Настройте:
   - **Токены HH** - введите все 4 токена в формате:
     ```
     hhtoken=ваш_токен
     hhul=ваш_токен
     crypted_id=ваш_токен
     xsrf=ваш_токен
     ```
   - **Resume Hash** - hash из URL вашего резюме (например, из `https://hh.ru/resume/HASH`)
   - **Сопроводительное письмо** - текст письма для откликов
   - **URL поиска** - ссылки на страницы поиска вакансий (можно несколько, каждое с новой строки)

### Команды бота

- `/start` - Главное меню
- **⚙️ Настройки** - Настройка параметров
- **📊 Статистика** - Просмотр статистики откликов
- **🚀 Запустить отклики** - Начать автоматическую обработку вакансий
- **📤 Поднять резюме** - Поднять резюме в поиске
- **❌ Остановить** - Остановить текущий процесс

## 🔧 Как это работает

1. **Браузерная автоматизация**: Бот использует Playwright для запуска реального браузера (headless режим)
2. **Авторизация**: Устанавливает cookies с токенами HH для авторизации
3. **Поиск вакансий**: Переходит на страницы поиска и собирает ID вакансий
4. **Отклики**: Открывает каждую вакансию, нажимает "Откликнуться", заполняет форму и отправляет
5. **Поднятие резюме**: Переходит на страницу резюме и нажимает кнопку "Поднять в поиске"

## 📁 Структура данных

Бот создаёт папку `data/` с файлами:
- `bot_config.json` - конфигурация бота
- `applied_vacancies.json` - список откликнутых вакансий
- `stats.json` - статистика работы

## ⚠️ Важные замечания

1. **Лимиты**: HH.ru имеет лимиты на количество откликов в день. Бот автоматически останавливается при достижении лимита
2. **Тесты**: Если вакансия требует прохождения теста, бот пропустит её и добавит в статистику
3. **Повторные отклики**: Бот автоматически пропускает вакансии, на которые уже откликались
4. **Задержки**: Между откликами есть задержка (по умолчанию 3 секунды) для избежания блокировок

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что токен Telegram бота правильный
- Убедитесь, что бот запущен

### Ошибки при откликах
- Проверьте, что токены HH актуальны (они могут истекать)
- Убедитесь, что resume_hash правильный
- Проверьте интернет-соединение

### Браузер не запускается
- Убедитесь, что установлены браузеры: `playwright install chromium`
- На Linux может потребоваться установка дополнительных библиотек

## 📝 Лицензия

Этот проект предназначен для личного использования. Используйте ответственно и соблюдайте правила использования hh.ru.
